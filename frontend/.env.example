# CyberCast Frontend Environment Configuration
VITE_API_BASE_URL=/api
# Set to false to connect to the live backend API.
# Set to true for offline demo fallback without a running backend.
VITE_USE_MOCK=false
