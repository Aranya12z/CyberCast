# Backend
# Default matches docker-compose.yml (postgres:16, local dev only — change in staging/prod)
DATABASE_URL=postgresql://user:password@localhost:5432/cybercast_db
JWT_SECRET=changeme-replace-in-production
ENV=development

# ML
MODEL_PATH=./ml/models/latest.pkl

# Notifications (FUTURE — no real SMS/email in MVP)
MESSAGING_API_KEY=

# Frontend
VITE_API_BASE_URL=http://localhost:8000/api